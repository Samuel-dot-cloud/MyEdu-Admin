<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.studiofive.myedu_admin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyEduAdmin">
        <activity android:name=".activities.QuestionDetailsActivity"
            android:theme="@style/Theme.MyEduAdmin.NoActionBar"></activity>
        <activity
            android:name=".activities.QuestionsActivity"
            android:theme="@style/Theme.MyEduAdmin.NoActionBar" />
        <activity
            android:name=".activities.SetsActivity"
            android:theme="@style/Theme.MyEduAdmin.NoActionBar" />
        <activity
            android:name=".activities.CategoryActivity"
            android:theme="@style/Theme.MyEduAdmin.NoActionBar" />
        <activity
            android:name=".activities.MainActivity"
            android:theme="@style/Theme.MyEduAdmin.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>